# RosterIQ Production Dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
httpx>=0.27.0
aiosqlite>=0.19.0
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# Database — legacy store (psycopg2) + integrations layer (asyncpg)
psycopg2-binary>=2.9
asyncpg>=0.29

# Connector credential encryption (Fernet)
cryptography>=42.0
